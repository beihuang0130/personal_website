# 📝 个人网页使用指南

## 🎯 快速开始

### 1️⃣ 修改个人信息

打开 `js/main.js` 文件，找到文件开头的 `personalData` 对象：

```javascript
const personalData = {
    name: "你的名字",              // 👈 改成你的真实姓名
    title: "你的职业/身份",         // 👈 改成你的职业
    bio: "个人简介...",            // 👈 写一段简短的自我介绍
    
    email: "your.email@example.com",
    location: "你的所在地",
    
    social: {
        github: "https://github.com/yourusername",
        linkedin: "https://linkedin.com/in/yourusername",
        email: "mailto:your.email@example.com"
    }
};
```

### 2️⃣ 添加技能

在同一个文件中，找到 `skills` 数组：

```javascript
skills: [
    { name: "前端开发", icon: "💻", level: 90 },
    { name: "UI/UX设计", icon: "🎨", level: 85 },
    // 👇 添加你自己的技能
    { name: "你的技能", icon: "🚀", level: 80 },
]
```

**图标选择**：
- 可以使用 emoji：💻 🎨 📱 ⚙️ 📊 🚀 🎯 ✨
- 或访问 [Emojipedia](https://emojipedia.org/) 查找更多

### 3️⃣ 添加项目

在 `projects` 数组中添加你的项目：

```javascript
projects: [
    {
        name: "我的项目名称",
        description: "项目的简短描述，介绍主要功能...",
        image: "images/projects/project1.jpg",  // 图片路径
        link: "https://github.com/yourusername/project",  // 项目链接
        tags: ["HTML", "CSS", "JavaScript"]  // 技术标签
    },
    // 添加更多项目...
]
```

### 4️⃣ 上传图片

#### 上传头像：
1. 准备一张头像图片（建议 400x400 像素）
2. 重命名为 `avatar.jpg`
3. 放入 `images/` 文件夹

#### 上传项目截图：
1. 准备项目截图（建议 1200x800 像素）
2. 重命名为 `project1.jpg`, `project2.jpg` 等
3. 放入 `images/projects/` 文件夹

## 🎨 自定义主题颜色

打开 `css/style.css`，找到 `:root` 部分：

```css
:root {
    --accent-primary: #6366f1;      /* 👈 主色调 */
    --accent-secondary: #8b5cf6;    /* 👈 次色调 */
    --accent-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

**推荐配色方案**：
- 蓝紫色（默认）：`#6366f1` → `#8b5cf6`
- 粉红色：`#ec4899` → `#f43f5e`
- 绿色：`#10b981` → `#14b8a6`
- 橙色：`#f59e0b` → `#f97316`

## 🚀 本地预览

### 方法1：直接打开
双击 `index.html` 文件在浏览器中打开

### 方法2：使用本地服务器（推荐）
```bash
# 使用 Python
python -m http.server 8000

# 或使用 Node.js
npx http-server
```

然后访问 `http://localhost:8000`

## 📤 推送到GitHub

```bash
# 1. 添加修改
git add .

# 2. 提交修改
git commit -m "更新个人信息"

# 3. 推送到GitHub
git push origin main
```

## 🌐 部署到GitHub Pages

1. 进入你的GitHub仓库页面
2. 点击 **Settings** (设置)
3. 在左侧菜单找到 **Pages**
4. 在 **Source** 下选择 `main` 分支
5. 点击 **Save** (保存)
6. 等待几分钟，你的网站将发布到：
   `https://beihuang0130.github.io/personal_website/`

## 🔧 常见问题

### Q: 图片不显示怎么办？
A: 检查以下几点：
- 图片路径是否正确
- 图片文件名是否匹配（区分大小写）
- 图片格式是否支持（.jpg, .png, .webp）

### Q: 如何修改网站标题？
A: 打开 `index.html`，找到 `<title>` 标签：
```html
<title>你的名字 - 个人网页</title>
```

### Q: 如何添加更多区块？
A: 在 `index.html` 中复制现有的 `<section>` 区块，然后修改内容。

### Q: 主题切换不工作？
A: 确保 `js/main.js` 文件已正确加载，检查浏览器控制台是否有错误。

## 📱 测试响应式设计

在浏览器中：
1. 按 `F12` 打开开发者工具
2. 点击设备工具栏图标（或按 `Ctrl+Shift+M`）
3. 选择不同设备查看效果

## 💡 优化建议

1. **压缩图片**：使用 [TinyPNG](https://tinypng.com/) 压缩图片
2. **性能测试**：使用 [PageSpeed Insights](https://pagespeed.web.dev/)
3. **SEO优化**：修改 `<meta>` 标签中的描述

## 🎉 完成！

修改完成后，记得：
1. 保存所有文件
2. 在浏览器中刷新查看效果
3. 推送到GitHub
4. 分享你的网站链接！

---

**需要帮助？** 查看 `README.md` 或在GitHub上提issue。
