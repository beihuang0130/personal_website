# 个人网页项目

这是一个现代化、响应式的个人网页项目，具有以下特性：

## ✨ 特性

- 📱 **完全响应式设计** - 支持手机、平板和电脑
- 🌓 **深色/浅色主题切换** - 自动保存用户偏好
- 🎨 **现代化设计** - 玻璃态效果、渐变色彩
- ✨ **流畅动画** - 滚动动画、悬停效果
- 🚀 **性能优化** - 快速加载、平滑交互
- 🎯 **SEO友好** - 语义化HTML结构

## 📁 项目结构

```
personal-website/
├── index.html          # 主页面
├── css/
│   └── style.css      # 样式文件
├── js/
│   └── main.js        # JavaScript逻辑
├── images/            # 图片资源文件夹
│   ├── avatar.jpg     # 📸 你的头像（请上传）
│   └── projects/      # 项目截图文件夹
│       ├── project1.jpg
│       ├── project2.jpg
│       └── project3.jpg
└── README.md          # 项目说明
```

## 🎯 如何修改个人信息

### 方法1：修改 JavaScript 配置（推荐）

打开 `js/main.js` 文件，在文件开头找到 `personalData` 对象，修改其中的内容：

```javascript
const personalData = {
    // 基本信息
    name: "你的名字",              // ✏️ 修改你的名字
    title: "你的职业/身份",         // ✏️ 修改职业
    bio: "个人简介...",            // ✏️ 修改简介
    
    // 联系信息
    email: "your.email@example.com",
    location: "你的所在地",
    
    // 社交媒体
    social: {
        github: "https://github.com/yourusername",
        linkedin: "https://linkedin.com/in/yourusername",
        email: "mailto:your.email@example.com"
    },
    
    // 技能列表
    skills: [
        { name: "技能名称", icon: "💻", level: 90 },
        // 添加更多技能...
    ],
    
    // 项目列表
    projects: [
        {
            name: "项目名称",
            description: "项目描述",
            image: "images/projects/project1.jpg",
            link: "项目链接",
            tags: ["标签1", "标签2"]
        },
        // 添加更多项目...
    ]
};
```

### 方法2：直接修改 HTML

如果你不熟悉JavaScript，也可以直接在 `index.html` 中查找带有 `✏️` 标记的注释，修改对应的内容。

## 📸 如何上传图片

### 上传头像：
1. 准备一张你的头像图片（建议正方形，至少 400x400 像素）
2. 将图片重命名为 `avatar.jpg`
3. 放入 `images/` 文件夹

### 上传项目截图：
1. 准备项目截图（建议 1200x800 像素）
2. 重命名为 `project1.jpg`, `project2.jpg` 等
3. 放入 `images/projects/` 文件夹
4. 在 `js/main.js` 中的 `projects` 数组中更新图片路径

## 🚀 如何使用

### 本地预览：
1. 直接双击 `index.html` 在浏览器中打开
2. 或使用本地服务器（推荐）：
   ```bash
   # 如果安装了 Python
   python -m http.server 8000
   
   # 或使用 Node.js 的 http-server
   npx http-server
   ```

### 部署到GitHub Pages：
1. 将代码推送到GitHub仓库
2. 在仓库设置中启用 GitHub Pages
3. 选择主分支作为源
4. 访问 `https://yourusername.github.io/repository-name`

## 🎨 主题定制

如需修改颜色方案，编辑 `css/style.css` 中的 CSS 变量：

```css
:root {
    --accent-primary: #6366f1;      /* 主色调 */
    --accent-secondary: #8b5cf6;    /* 次色调 */
    --accent-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    /* 更多颜色... */
}
```

## 📱 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge
- 移动端浏览器

## 📄 许可证

MIT License - 自由使用和修改

## 🤝 贡献

欢迎提出问题和建议！

---

**祝你使用愉快！如有问题，请随时联系。** 🎉
